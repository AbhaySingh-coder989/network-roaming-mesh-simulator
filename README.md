# network-roaming-mesh-simulator
A Java-based simulation that fixes the 'Sticky Client' Wi-Fi bug. Uses a Graph to map out routers, a PriorityQueue to sort signal strengths, and BFS/Dijkstra algorithms to find the shortest path back to the main internet modem.
